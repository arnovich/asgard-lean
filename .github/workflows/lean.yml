name: Lean verification

on:
  push:
    branches: [main]
    paths-ignore: ["tasks/**", "docs/**"]
  workflow_dispatch:

concurrency:
  group: lean-verification-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lean:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Install Lean toolchain manager
        shell: bash
        run: |
          curl --fail --silent --show-error --location \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            --output "$RUNNER_TEMP/elan-init.sh"
          sh "$RUNNER_TEMP/elan-init.sh" -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Fetch pinned mathlib build cache
        run: lake exe cache get
      - name: Check libraries, examples and regression proofs
        run: lake build
      - name: Compile optional executable entry points
        run: lake build simulation_demo simulation_process_tests rational_simulation_demo three_state_demo
